// Vercel executes this entry point as native ESM. Keep the explicit source
// extension so its function bundler traces and compiles the server module.
import app from '../server.ts';

export default app;
